# terraform-learning-project


terraform fmt = formatieren des Codes

terrafrom init = initialisierung 

terraform plan -out=tfplan : um konfiguration zu speichern

env | grep AWS zeig nutzer daten an

terraform destroy = aufräumen